# 贡献指南

感谢你帮助改进抖音剪映信息流自动制作Skill。

## 提交Issue前

请先说明：

- Windows版本。
- 剪映专业版版本。
- n8n版本及部署方式。
- Codex或Computer Use环境。
- 使用兼容模式还是加速模式。
- 期望结果、实际结果和可复现步骤。

不要上传真实用户素材、口播文案、剪映草稿、账号信息、API密钥或含个人信息的截图。可以使用自行创建的匿名测试素材复现。

## Pull Request要求

- 每个PR聚焦一个问题。
- 不改变固定成片参数，除非PR明确提供兼容配置并说明理由。
- 精确媒体处理应使用确定性脚本，不要只增加模糊的自然语言要求。
- 新脚本必须提供最小可复现测试方法。
- 不得把whisper模型、剪映安装包、音乐文件、用户素材或其他大体积二进制文件提交到仓库。
- 不得包含密码、令牌、Cookie、Webhook密钥、私有路径或客户信息。
- 修改Skill路由时，确保兼容模式、加速模式和停止条件仍然清晰。

## 字幕相关贡献

字幕实现必须满足：

- 最终文字只来自原始文案。
- whisper.cpp识别文本只能作为对齐依据。
- 每段字幕严格一行。
- 字幕顺序连接后与 `narration_text` 完全一致。
- 不漏首句和末句，时间不重叠。
- 失败时输出可检查的对齐报告，不能静默修正。

## 视频处理相关贡献

- 输出保持9:16、1080×1920和30fps要求。
- 时长误差不超过1帧。
- 不截断口播。
- 不产生黑帧、空白段或长时间定格。
- 视频短缺超过2秒时使用内容相关素材补齐。
- 不用FFmpeg烧录文字替代剪映标题或字幕预设。

## 提交前检查

1. 确认没有敏感文件和大模型文件。
2. 检查Skill frontmatter、文件链接和资源路径。
3. 运行新增或修改脚本的实际测试。
4. 更新相关文档，但不要重复粘贴同一规则。
5. 在PR中说明测试范围、已知限制和回退方式。

