---
name: douyin-jianying-video-workflow-agent
description: 使用 n8n、确定性本地脚本、whisper.cpp、FFmpeg、本地视频素材和剪映专业版制作抖音信息流视频。适用于素材入库、文案解析、制作任务生成、剪映文本朗读、精确字幕、视频预处理、标题字幕预设、音视频对齐、草稿验收和按授权导出的完整流程。
---

# 抖音剪映信息流视频自动制作 Agent

## 目标

把用户提供的视频素材和口播文案转换为可复核的剪映专业版竖屏草稿。n8n 负责监控文件、解析文案和生成结构化任务；具备 Windows 桌面控制能力的 Codex 负责在剪映中完成实际制作。

不得把“生成了 n8n 任务”表述为“已经完成剪映成片”。只有剪映草稿经过时间线和画面验收后，才能报告制作完成。

## Agent 系统组成

本 Skill 是 Codex 视频制作 Agent 的操作规范和能力入口，不把自身表述成独立 Agent：

- `Codex`：读取任务、选择执行模式、调用工具、处理异常并验收结果。
- `SKILL.md`：保存不可变参数、流程选择、权限边界、停止条件和完成标准。
- `n8n`：监控输入、生成任务、调度本地阶段和记录状态；不直接点击剪映。
- `确定性本地脚本`：完成字幕对齐、SRT 生成、视频处理和机器可验证的检查。
- `whisper.cpp`：从剪映最终口播音频取得语音时间点；识别文字不得直接作为最终字幕。
- `FFmpeg/FFprobe`：读取媒体时长、静音、转码、裁剪和按规则补足视频。
- `Computer Use`：只处理必须在剪映界面完成的配音、预设、导入、音乐和视觉验收。

不要让语言模型临时重写可重复的精确算法。已存在并通过验证的脚本必须优先于临时命令；脚本不存在或未验证时，不能假装已调用。

## 默认目录

默认工作区为 `D:\抖音信息视频工作流`：

- `01_待入库`：用户新放入的原始视频。
- `02_素材库`：已扫描、已登记的视频素材及 `视频素材库.json`。
- `03_口播文案`：标题和口播正文文件。
- `04_制作任务`：n8n 生成的 `TASK-*\task.json`。
- `05_剪映草稿`：草稿交付信息或草稿映射。
- `06_成片`：经过用户授权后导出的成片。
- `scripts`：n8n 调用的本地执行脚本。
- `workflows`：可导入 n8n 的工作流 JSON。

若同事电脑的根目录不同，先确认新根目录，再统一替换；不要在 C 盘新建素材库、草稿库、缓存库或成片目录。

## 输入约定

默认允许把全部口播任务集中放在 `03_口播文案\批量视频文案.txt`，每条任务必须使用下列分隔格式：

```text
【任务开始】
视频：素材库中的完整视频文件名.mp4
标题：该条视频标题
正文：该条视频的完整口播文案。
【任务结束】
```

需要新建总文档时，复制并填写[批量文案模板](assets/batch-voiceover-template.txt)。

- 一个总文档可以包含多组任务；新增任务只能追加到文档末尾，不要插入、重排或复用旧任务块。
- `视频：`必须填写 `01_待入库` 中的完整文件名和扩展名。单任务文档可省略该字段并使用与视频相同的基本文件名；批量文档不得省略。
- 每组任务独立校验和生成 `TASK-*`。某一组格式错误只能阻止该组，不得阻止其他有效组。
- 批量任务 ID 必须根据“匹配后的视频文件名 + 标题 + 规范化正文”逐条计算哈希，不得使用整个总文档哈希；向末尾追加内容时，旧任务不得重复创建。
- 删除总文档中的旧任务不得自动删除已生成的任务。修改旧任务内容应视为新版本并生成新的任务 ID，旧任务保留供追踪。
- 忽略 `~$` 开头的 Word 临时文件；文件尚未保存完成或修改时间不足 5 秒时暂不处理。
- 文案首选 UTF-8 `.txt`，也支持 `.docx` 和 `.doc`。标题或正文缺失、批量分隔符不成对、视频不存在或匹配到多个视频时，停止该组并记录具体原因，不要自行补写或猜测。
- 字幕分段只能拆分原文，不得改写字符或标点；全部分段连接后必须与规范化口播正文完全一致，校验失败时不得创建制作任务。

兼容旧版的单任务文档格式：

```text
视频：可选的视频文件名.mp4
标题：该条视频标题
正文：该条视频的完整口播文案。
```

视频素材由用户放入素材库。不要使用图片补画面。背景音乐固定使用下方指定的剪映收藏音乐；只有用户明确要求更换时才改用其他曲目。

## 执行模式选择

开始任务前检查组件并选择一种模式：

1. `加速模式`：n8n、FFmpeg/FFprobe、whisper.cpp、可用的字幕对齐与视频预处理脚本、剪映母版和 Computer Use 全部可用。按[加速执行流程](references/accelerated-execution.md)执行。
2. `兼容模式`：加速脚本尚未安装或未通过测试，但 n8n、剪映和 Computer Use 可用。按下方“兼容执行流程”完成，不得声称使用了 whisper.cpp 或自动视频预处理。
3. `停止`：缺少最终口播音频、无法定位配音文件、目标预设或指定音乐不可用、Computer Use 无法可靠获取剪映窗口，或者关键验收失败。报告具体缺失项和已完成阶段。

模式必须写入任务记录。加速模式中每个阶段都应产生可检查的中间文件或状态；不得因为后续失败而重复创建相同任务或覆盖源素材。

## 固定剪映参数

### 画布与声音

- 画布：`9:16`，`1080 × 1920`，`30 fps`。
- 口播：剪映“文本朗读”。
- 音色：`解说小帅`。
- 口播速度：`1.1x`。
- 原视频声音：静音。
- 背景音乐：剪映 `音频 → 我的 → 收藏` 中的 `舒缓 煽情 唯美 浪漫 轻音乐`，作者 `Lance`，素材库显示时长 `02:52`。
- 音乐视觉参照：[固定背景音乐截图](assets/background-music-reference.png)。若同名音乐不可用或尚未下载，停止并报告，不能擅自替换。
- 横屏素材：清晰主画面居中；使用同一视频的模糊背景填满上下区域，不得保留黑边。

### 标题

- 标题文字取自文案标题，不要改写。
- 文字样式必须直接点击用户在剪映中确认的预设；在当前剪映版本的预设网格中为第一行第 4 个黄色 `T` 缩略图，效果为黄色文字、黑色描边。应用预设后，不得再手动修改颜色、描边、背景、阴影、发光等样式细项。以[标题样式和位置参照](assets/title-style-position-reference.png)为最终视觉标准，不得改用相似模板。
- 字体：`得意黑`。
- 字号：`15`。
- 位置：在 `1080 × 1920` 竖屏画布中设为 `X = 0`、`Y = 854`。标题水平居中，位于上方模糊背景与清晰主画面的分界线稍上方，实际画面位置以[标题样式和位置参照](assets/title-style-position-reference.png)为准。
- 时长：从 `00:00:00` 开始，持续到主口播音频结束。

若剪映要求下载字体或预设，先完成剪映内下载再应用。若目标版本确实没有该预设，停止并报告，不能静默替换为相似模板。

### 字幕

- 字幕时间点：以剪映生成的最终口播音频识别，不使用字符数估算作为最终时间轴。
- 字幕文字：必须直接取自原始口播文案。自动识别结果只用于获得时间点，不能作为最终文字来源。
- 字体：`得意黑`。
- 字号：`10`。
- 文字颜色：`白色`。
- 样式：使用剪映基础字幕样式，不使用任何带描边的字幕预设。字幕必须为纯白文字，关闭描边、背景、阴影、发光和实心底框。只设置得意黑、10号字、白色和规定坐标，不添加其他装饰。原[字幕位置参照](assets/caption-style-position-reference.png)仅用于确认画面位置，其中旧的浅粉描边效果已经停用，不得继续套用。
- 位置：在 `1080 × 1920` 竖屏画布中设为 `X = 102`、`Y = -810`。字幕位于清晰主画面下方的模糊背景区域上沿附近，实际画面位置以[字幕样式和位置参照](assets/caption-style-position-reference.png)为准。
- 每段字幕只能显示一行。若一句过长，拆成多个连续字幕片段，不能在同一个字幕片段内换行。
- 不得出现错字、同音字替换、漏字、增字、改写或标点变化。
- 按时间顺序连接全部字幕文字后，必须与任务中的 `narration_text` 完全一致。
- 字幕必须覆盖完整口播，不能漏掉首句或末句；完成后必须逐字校对。

## 音视频等长规则

以剪映生成的“解说小帅”口播音频为唯一主时钟：

1. 口播音频、背景音乐、主视频轨、模糊背景、标题轨的开始时间全部为 `00:00:00`。
2. 背景音乐、主视频轨、模糊背景和标题轨的总时长必须与口播音频总时长一致。
3. 在 30 fps 下允许的最大结束时间误差为 1 帧，即约 `0.033 秒`。
4. 视频长于音频：在音频结束点裁剪视频。
5. 视频短于音频且缺口不超过 2 秒：可延长当前素材可用尾部、使用短片段衔接或末帧定格补齐。
6. 视频短于音频且缺口大于 2 秒：必须从 `02_素材库` 选择内容相关的视频并截取片段补齐；所截片段合计时长应覆盖缺口，不能用长时间定格代替。
7. 不得为了迁就视频长度截断口播音频，也不得留下空白画面或黑帧。
8. 最终字幕时间以口播音频识别结果为准，估算分段只用于生成初始任务；字幕文字始终以原文为准。
9. 背景音乐原始时长较长时，从开头使用并在口播音频结束点裁剪；不得让音乐比口播提前结束或继续延长。

## 兼容执行流程

1. 检查 `01_待入库`、`03_口播文案` 和 n8n 是否可用。
2. 运行或确认 `01-视频素材自动入库-v1`，更新 `02_素材库\视频素材库.json`。
3. 运行或确认 `02-口播文案解析-v1`，为每份新文案生成独立 `TASK-*` 目录和 `task.json`。
4. 读取任务标题、正文、素材要求和剪映默认参数；不要仅依赖聊天记忆。
5. 打开剪映专业版，新建或打开该任务草稿，导入视频素材。
6. 先生成完整口播音频并设置 `解说小帅`、`1.1x`，读取其最终准确时长。
7. 按“音视频等长规则”整理主视频和模糊背景。
8. 添加标题，直接点击指定标题预设；只另外设置得意黑、15 号字和坐标 `X = 0`、`Y = 854`，不要手调预设内部样式；标题覆盖全程。
9. 根据最终口播音频生成字幕时间点，用原始文案逐段替换识别文字；全部字幕使用基础样式，设置得意黑、10号字、纯白文字和坐标 `X = 102`、`Y = -810`，关闭描边、背景、阴影和发光；每段只允许一行。
10. 静音原视频；从剪映收藏中加入指定的 `舒缓 煽情 唯美 浪漫 轻音乐`，并在口播结束点裁剪。
11. 逐项验收并保存草稿。只有用户明确要求导出，或任务中存在 `export.approved: true` 时，才能导出到 `06_成片`。
12. 将实际口播时长、草稿路径、验收结果和最终状态回填任务记录。

兼容模式是可靠回退，不是失败。它允许完成草稿，但速度通常低于加速模式。

## n8n 要求

- n8n 本地地址通常为 `http://localhost:5678`。
- 文案工作流默认每 5 分钟检查一次；同时保留手动触发入口。
- 本项目的 `02-口播文案解析-v1` 调用 `scripts\sync_voiceover_tasks.js`，因此参数修改应优先落到这个脚本，避免只改画布上的节点说明。
- 批量文案必须由随 Skill 提供的 `scripts\sync_voiceover_tasks.js` 逐块解析。安装到工作区时应复制或同步该脚本到 `D:\抖音信息视频工作流\scripts\sync_voiceover_tasks.js`，并通过隔离样例验证后再启用定时触发。
- 安装包更新后，优先运行 `scripts\install-workspace.ps1 -WorkspaceRoot "D:\抖音信息视频工作流" -Force` 同步工作区脚本和模板。安装器覆盖旧脚本前必须保留时间戳备份，不得删除用户素材、文案、任务、草稿或成片。
- 每个任务必须保持幂等：相同源文件哈希不得重复创建多个任务。
- n8n 免费本地版可以运行这些文件工作流；不需要 GPT API 才能完成素材扫描、文案解析和任务生成。
- n8n 本身不能原生点击剪映。若目标 Codex 没有 Windows 桌面控制能力或本地剪映执行器，生成任务后停止并明确报告缺少的控制组件。
- n8n 只负责调度可重复阶段；剪映界面状态不得只依赖 n8n 节点名称推断。
- 加速模式的推荐任务状态为：`等待处理 → 配音已生成 → 字幕已生成 → 视频已处理 → 等待剪映装配 → 草稿已完成 → 等待人工确认`。
- 每个状态更新前必须验证对应产物存在且通过该阶段检查；失败时记录失败阶段和原因，不得跳到后续状态。

## Computer Use 提速规则

- 固定剪映窗口大小、系统缩放、剪映版本和面板布局；坐标只对已验证的界面配置有效。
- 获取一次界面状态后，批量执行同一稳定面板内的点击、输入和快捷键；不要每次点击后都重新截图。
- 只在阶段开始、界面可能变化、出现弹窗、用户打断或阶段完成验收时重新获取界面状态。
- 标题必须使用剪映指定预设；字幕使用剪映基础样式并批量应用纯白、无描边参数。不得用 FFmpeg 把文字烧录进视频替代剪映文本轨。
- 剪映更新、预设网格变化、弹窗遮挡或坐标不再可靠时立即停止，不得用旧坐标盲点。

## 完成验收

只有下列各项全部满足，才可把草稿状态标记为“剪映草稿已完成”：

- 标题文字与文案一致。
- 标题已直接应用指定预设，未手调预设内部样式；字体为得意黑、字号 15、坐标 `X = 0`、`Y = 854`，并覆盖全程，视觉效果与标题参照截图一致。
- 全部字幕均为纯白文字，没有描边、背景、阴影或发光；字体为得意黑、字号10、坐标 `X = 102`、`Y = -810`，每段严格一行。旧字幕参照截图只用于位置，不用于样式。
- 画布为 9:16，横屏主画面清晰居中，上下为模糊背景，无黑边。
- 口播为解说小帅、1.1x，原视频静音；背景音乐为指定的 `舒缓 煽情 唯美 浪漫 轻音乐`。
- 背景音乐、视频、模糊背景、标题和口播音频结束时间误差不超过 1 帧。
- 字幕覆盖完整口播；全部字幕按顺序连接后与 `narration_text` 完全一致，不允许任何错字、漏字、增字或标点变化。
- 视频短于口播超过 2 秒时，已从素材库截取相关片段补齐，没有使用长时间定格敷衍。
- 草稿已保存到 D 盘约定目录；源素材和文案未被删除或覆盖。

如果任一项未通过，报告未通过项和所需修正，不要声称任务已完成。
